# Ignore ONLY the backend API at the project root
/api
/api/**

# Make sure we do NOT ignore the frontend helpers under src/api
!src/api/**
!src/api

# Optional: ignore server-only artifacts
/uploads
/Dockerfile
*.db
*.sqlite
*.sql